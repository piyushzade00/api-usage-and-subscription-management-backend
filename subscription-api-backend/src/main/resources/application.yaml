spring:
  application:
    name: Subscription API Backend

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        format_sql: false
        jdbc:
          time_zone: UTC
    defer-datasource-initialization: true

  profiles:
    active: dev

server:
  port: 8080

logging:
  level:
    root: INFO
    org.springframework.web: INFO
    org.flywaydb: INFO
    org.springframework.boot.context.config: DEBUG